const { PRIORITY_BELOW_NORMAL } = require("constants");

const test = {
    test: "letind",
    sum: function(a, b) {
        return a+b;
    }
}
let ss = test.sum(12, 3);

console.log(ss);

const a = {
    name : "본인이름",
    gender : "본인성별",
    age: "본인성별"
}

// 계산기

// 객체이름 calculator
